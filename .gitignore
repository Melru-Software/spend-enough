# OS / editor cruft
.DS_Store
*.swp
*~
.vscode/
.idea/

# Node (for tests / tooling only — app itself has no build step)
node_modules/
npm-debug.log*

# Local preview tooling (absolute paths, not portable)
.claude/
